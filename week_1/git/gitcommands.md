git status (show latest changes)
git log
git log --oneline
git checkout <branch>
git commit -m "msg"
git push origin master (git push if using ssh)
git pull origin master
git stash ("stashes" changes for later)
git ls-files
